<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Разрешения приложения: интернет, уведомления, запуск по расписанию -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"
        tools:ignore="ExactAlarmPolicy" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gymnasium.Splash">

        <!-- Загрузчик всего приложения -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.Gymnasium.Splash">

            <!-- Определение главного Activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Открытие ссылок в приложении -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- gmn://open/ — открывает приложение -->
                <data
                    android:scheme="gmn"
                    android:host="open"
                    android:path="/" />

            </intent-filter>

        </activity>

        <!-- Другие Activity приложения -->
        <activity android:name=".ui.MainActivity" android:exported="false" />
        <activity android:name=".ui.LoginActivity" android:exported="false" />

        <!-- Receiver для запуска по расписанию (уведомления с напоминаниями) -->
        <receiver
            android:name=".utilities.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Service для уведомлений -->
        <service android:name=".utilities.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>